You are a senior Python architect specializing in FastAPI backend restoration and Replit project setup.  
I’ve uploaded a ZIP file containing my entire Vinted automation bot (previously working on another Replit account).  
Your task is to **fully reconstruct** the project environment and make it ready for deployment with “Always On”.

Here’s exactly what I want you to do step-by-step:

1️⃣ **Unzip & Structure**
- Unzip the uploaded file into the current Replit workspace.
- If the project folder structure is nested (e.g. “mybot/mybot/”), move all source files (backend, start_bot.bat, requirements.txt, etc.) to the root of the Repl.
- Ensure the folder structure looks like this:
/backend/
├── app.py
├── routes/
├── services/
├── models/
├── jobs/
├── data/
requirements.txt
.env.example
start_bot.bat
README.md

markdown
Copier le code

2️⃣ **Environment Setup**
- Create a new Python virtual environment if needed.
- Install all dependencies from `requirements.txt` using `pip install -r requirements.txt`.
- If requirements.txt doesn’t exist, auto-detect dependencies (FastAPI, Uvicorn, Pydantic, etc.) and regenerate the file.
- Validate installation by running:
python -m uvicorn backend.app:app --host 0.0.0.0 --port 8000

markdown
Copier le code
→ Confirm the API responds with `{ "status": "healthy" }` at `/`.

3️⃣ **Environment Variables**
- Create a `.env` file using `.env.example` as a template.
- Add placeholders for optional keys:
OPENAI_API_KEY=
PRICE_DROP_CRON=0 3 * * *
ALLOWED_ORIGINS=https://lovable.dev,https://localhost:3000

markdown
Copier le code

4️⃣ **Replit Configuration**
- Create or update `.replit` and `replit.nix` files so that the Repl automatically runs:
uvicorn backend.app:app --host 0.0.0.0 --port 8000

markdown
Copier le code
- Ensure the `Run` button launches the backend directly.
- Add persistent storage setup so `/backend/data` isn’t reset on each restart.

5️⃣ **Test and Verify**
- Check that the FastAPI `/docs` endpoint loads correctly on the Replit public URL (e.g., `https://<replname>.replit.app/docs`).
- Verify that POST `/ingest/photos` and POST `/listings/publish/{item_id}` both work and return 200 OK.

6️⃣ **Prepare for Deployment**
- Enable HTTPS and “Always On” under the Replit Deployments panel.
- Make sure CORS headers are configured to accept requests from `https://lovable.dev`.
- Print the final public API URL at the end.

7️⃣ **Documentation**
- Regenerate a clean README.md with:
- Full installation steps
- Endpoint usage
- Deployment instructions
- Example `.env` config

Once everything is complete, summarize all fixed paths, detected issues, and confirm the app is running on the final public URL.
⚡️ Résultat attendu après envoi du prompt :
✅ Ton ZIP sera décompressé et nettoyé automatiquement
✅ Le backend FastAPI sera opérationnel sur Replit (port 8000 ou 8001)
✅ L’IA te donnera l’URL publique Replit.app
✅ Tu pourras directement connecter Lovable avec VITE_API_BASE_URL=https://...replit.app